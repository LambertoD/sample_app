<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
     <!-- Replace with render... following moved to app/views/users/_user.html.erb.  Rails knows to route if used "render user"  or "render @users"	
    <li>
  	  <%= gravatar_for user, size: 52 %>
  	  <%= link_to user.name, user %>
    </li>
  	-->
  	  <%= render @users %>

  <% end %>
</ul>

<%= will_paginate %>